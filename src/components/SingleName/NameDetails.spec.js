describe('NameDetails', () => {
  it.todo(
    'should redirect to /register if register tab and is not an absolute path'
  )
  it.todo(
    'should redirect to /details if details tab and is not an absolute path'
  )
  it.todo(
    'should redirect to /subdomains if subdomains tab and is not an absolute path'
  )
})
