import { useResetFormOnNetworkChange } from './Records'

describe('useResetFormOnNetworkChange', () => {
  it.todo('should reset records when there is a network change')
  it.todo('should close confirm modal when there is a network change')
})
