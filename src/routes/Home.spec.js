import { handleDisconnect } from './Home'

describe('Home', () => {
  it.todo(
    'should not show MyAccount if we have accounts but are in readOnly mode'
  )
})

describe('handleDisconnect', () => {
  it.todo('should call disconnect on web3modal')
  it.todo('should reset the appropriate reactive vars')
})
